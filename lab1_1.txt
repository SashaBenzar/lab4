#include <stdio.h>
#include <math.h>

int main()
{
    int x,z,y,er;
    do
    {
    er=0;
    printf("введіть x=");
    scanf("%d",&x);
    printf("введіть z=");
    scanf("%d",&z);
    if(sqrt(x)-2*sqrt(x*z)==0)
    {
        printf("помилка\n");
        er=1;
    }
    else if(x==0)
    {
        printf("помилка\n");
        er=1;
    }
    }while(er==1);
    y=sqrt(x)+2*sqrt(x*z)/sqrt(x)-2*sqrt(x*z);
    printf("%d",y);
    return 0;
}